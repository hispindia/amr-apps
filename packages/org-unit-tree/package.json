{
    "name": "@hisp-amr/org-unit-tree",
    "version": "0.0.3",
    "description": "",
    "license": "BSD-3-Clause",
    "main": "dist/index.js",
    "scripts": {
        "transpile": "babel src -d dist",
        "start": "babel src -d dist -w",
        "build": "NODE_ENV=production BABEL_ENV=production babel src -d dist",
        "prebuild": "rimraf dist",
        "prestart": "rimraf dist",
        "test": "react-scripts test"
    },
    "dependencies": {
        "@hisp-amr/api": "*",
        "@dhis2/ui-core": "^3.9.1",
        "styled-components": "^5.0.0-beta.8"
    },
    "peerDependencies": {
        "react": "^16.9.0"
    }
}
